// What will be displayed on the screen when the given program is executed?
// The numbers 1, 2, 3, 4 and 5 are input

#include <iostream>
using namespace std;
int a, b;
int main() {
    cin >> a >> b >> a >> b >> a;
	cout << a << " " << b << " " << a << " " << b << " " << a << endl;
    return 0;
}